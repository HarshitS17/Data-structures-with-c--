#write a python program to count the occurence of letters in a given sentence 

sentence = input("enter a sentence ")

letterCount = {}

for letter in  sentence :
    letter = letter.lower()
    letterCount[letter] = 
    
    
#write a python ptogram to remove the characters that have odd index values in a given string.
# Input from the user
input_string = input("Enter a string: ")

# Create a new string with characters that have even index values
result_string = input_string[::2]

print("String after removing characters with odd index values:", result_string)

#Write a python script that takes input from the user and display the input back in the upper and lower cases
# WAP that was a lemda funtion to square each element in a fn 
# WAP function that captilize each first letter of element 

# Input: List of numbers
numbers = [1, 2, 3, 4, 5]

# Use map with a lambda function to square each element
squared_numbers = 
